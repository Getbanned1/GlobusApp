<Window x:Class="GlobusApp.ApplicationEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GlobusApp"
        mc:Ignorable="d"
        Title="Редактировать заявку" Height="400" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Клиент -->
        <Label Content="Клиент:"/>
        <ComboBox Grid.Row="0" x:Name="UserComboBox"
                  DisplayMemberPath="FullName"
                  SelectedValuePath="Id"
                  Margin="120,0,0,0"/>

        <!-- Тур -->
        <Label Grid.Row="1" Content="Тур:"/>
        <ComboBox Grid.Row="1" x:Name="TourComboBox"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  Margin="120,0,0,0"
                  SelectionChanged="TourComboBox_SelectionChanged"/>

        <!-- Количество человек -->
        <Label Grid.Row="2" Content="Количество человек:"/>
        <TextBox Grid.Row="2" x:Name="NumberOfPeopleTextBox"
                 Margin="120,0,0,0"
                 TextChanged="NumberOfPeopleTextBox_TextChanged"/>

        <!-- Статус -->
        <Label Grid.Row="3" Content="Статус:"/>
        <ComboBox Grid.Row="3" x:Name="StatusComboBox"
                  DisplayMemberPath="Status"
                  SelectedValuePath="Id"
                  Margin="120,0,0,0"/>

        <!-- Итоговая стоимость -->
        <Label Grid.Row="4" Content="Итоговая стоимость:"/>
        <TextBlock Grid.Row="4" x:Name="TotalCostTextBlock"
                   Margin="120,0,0,0"
                   FontWeight="Bold"/>

        <!-- Комментарий -->
        <Label Grid.Row="5" Content="Комментарий:"/>
        <TextBox Grid.Row="5" x:Name="CommentTextBox" Margin="120,0,0,0" Height="60" TextWrapping="Wrap"/>

        <!-- Сохранить -->
        <Button Grid.Row="6" Content="Сохранить" Width="100" Height="30"
                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Click="SaveButton_Click"/>
    </Grid>
</Window>
